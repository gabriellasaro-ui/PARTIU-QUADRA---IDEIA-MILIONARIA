fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0
pydantic-settings==2.5.2
email-validator==2.2.0

# Banco e migrações
sqlalchemy==2.0.35
psycopg[binary]==3.2.1
alembic==1.13.2

# Cache, filas e tempo real
redis==5.0.8
celery==5.4.0

# Autenticacao
bcrypt==4.2.0
pyjwt==2.9.0
google-auth==2.34.0
# Transporte HTTP do google-auth. Sem ele, o import de
# google.auth.transport.requests estoura ImportError e o login Google
# falha com a mesma mensagem de "token invalido" — dependencia ausente
# disfarcada de token ruim.
requests==2.32.3

# Push (Fase 7) — FCM real; import lazy apenas quando FCM_CREDENTIALS_PATH
# estiver configurado. O provider mock nao importa firebase-admin.
firebase-admin==6.5.0

# Cliente HTTP para os servicos de terceiro: ViaCEP (endereco), BrasilAPI
# (CNPJ + CNAE) e Resend (codigo de verificacao por e-mail).
#
# ⚠️ ESTAVA FALTANDO, e o modo de falhar foi o pior possivel: aqui o httpx
# entra na venv de carona com o TestClient do pytest, entao TUDO passava em
# desenvolvimento. No container, que instala so este arquivo, o
# `import httpx` estourava e as tres rotas respondiam 500 — busca de CEP,
# consulta de CNPJ e envio de codigo. Dependencia de producao nao pode
# depender de ferramenta de teste para existir.
httpx==0.28.1

# Rate limiting (Fase 12) — slowapi + limits com storage no Redis
# (in_memory_fallback_enabled=True quando o Redis cai).
slowapi==0.1.9
limits==3.13.0

