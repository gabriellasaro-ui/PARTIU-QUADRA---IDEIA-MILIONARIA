/* Configuracao real do painel do admin. Default: backend de dev local;
   em producao troque por https://api.qadras.com.br. */
window.__PQ_CONFIG__ = {
  API_BASE_URL: 'http://localhost:8000',
  STORAGE_PREFIX: 'pq'
};
